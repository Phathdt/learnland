server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: serve index.html for client-side routes.
    # API calls go directly to the backend (CORS-enabled), not through nginx.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
